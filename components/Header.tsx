import React from 'react'

type Props = {}

const Header = (props: Props) => {
  return (
    <header className='p-10 text-white text-2xl bg-blue-500'>
      Header
    </header>
  )
}

export default Header